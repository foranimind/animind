# apps/web 前端文件结构

说明: 基于当前源码内容整理; `.vite`、`node_modules`、`dist` 为运行时/依赖/构建产物, 未展开列出。

## Tree (含功能/内容简述)

```Plaintext
apps/web/
├── .env.example                # 环境变量示例 (VITE_API_BASE / VITE_USE_MOCK / VITE_DEV_PROXY_TARGET)
├── .vite/                      # Vite 缓存产物/中间文件
├── dist/                       # 构建产物 (vite build 输出)
├── node_modules/               # 依赖安装目录 (npm install 生成)
├── public/
│   └── mock/
│       └── assets/
│           └── demo_job/
│               ├── manifest.json            # Mock 模式示例 manifest
│               ├── motion/
│               │   └── motion.bvh           # Mock 动作数据
│               ├── music/
│               │   └── music.wav            # Mock 音频
│               ├── preview/
│               │   └── preview_config.json  # Mock 预览配置
│               └── scene/
│                   └── panorama.png         # Mock 全景图
├── src/
│   ├── __tests__/
│   │   ├── LibraryFilterDrawer.test.tsx     # 筛选抽屉交互测试
│   │   ├── SelectMenu.test.tsx              # SelectMenu 组件测试
│   │   ├── WorkCard.test.tsx                # 作品卡片渲染测试
│   │   ├── status.test.ts                   # 状态标签/色调逻辑测试
│   │   ├── useBodyScrollLock.test.tsx       # 滚动锁定 Hook 测试
│   │   └── useDismissable.test.tsx          # 点击外部/ESC 关闭 Hook 测试
│   ├── App.tsx                  # 路由入口: / /works /works/:id
│   ├── env.d.ts                 # Vite 环境变量类型声明
│   ├── global.d.ts              # CSS 变量/全局类型声明
│   ├── main.tsx                 # React 渲染入口 + BrowserRouter
│   ├── setupTests.ts            # Vitest + Testing Library 全局配置
│   ├── components/
│   │   ├── chat/
│   │   │   └── CreateChatPanel.tsx          # 创作页聊天区 + 提示词输入
│   │   ├── inspector/
│   │   │   ├── InspectorOptionsPanel.tsx    # 参数选择面板
│   │   │   ├── InspectorProgressPanel.tsx   # 生成进度 + 日志面板
│   │   │   └── InspectorResultsPanel.tsx    # 预览/素材/导出结果面板
│   │   ├── layout/
│   │   │   ├── AppLayout.tsx                # 页面布局壳: 侧边栏 + Outlet
│   │   │   └── layout.css                   # 全局布局/容器样式
│   │   ├── sidebar/
│   │   │   ├── AppSidebar.tsx               # 侧边栏: 导航/会话/收藏
│   │   │   └── sidebar.css                  # 侧边栏样式
│   │   ├── library/
│   │   │   ├── LibraryCommandBar.tsx        # 搜索 + 筛选按钮 + chip
│   │   │   ├── LibraryFilterDrawer.tsx      # 筛选抽屉 (ESC/点击外部关闭)
│   │   │   ├── LibraryFilters.tsx           # 筛选项表单 (SelectMenu)
│   │   │   ├── WorkCard.tsx                 # 作品卡片: 预览/状态/元信息
│   │   │   └── library.css                  # Library 页面卡片样式
│   │   ├── preview/
│   │   │   ├── PreviewPanel.tsx             # 预览面板: 场景/音乐/动画
│   │   │   ├── PreviewControls.tsx          # 播放/暂停/音量/进度控制
│   │   │   ├── PreviewScene.tsx             # Three.js 渲染: BVH + 全景
│   │   │   ├── ThreePreviewPlayer.tsx       # Three 预览播放器 (音频同步/进度)
│   │   │   └── preview.css                  # 预览面板样式
│   │   └── ui/
│   │       └── SelectMenu.tsx    # 自定义下拉选择组件
│   ├── hooks/
│   │   ├── useAsync.ts           # 异步加载状态 Hook (useResource)
│   │   ├── useBodyScrollLock.ts  # body 滚动锁定
│   │   ├── useDismissable.ts     # 点击外部/ESC 关闭逻辑
│   │   ├── useJobRunner.ts       # 提交任务 + SSE/WS 订阅/轮询兜底
│   │   ├── usePreviewConfig.ts   # 加载 preview_config (状态管理)
│   │   ├── useRecentWorks.ts     # 最近作品列表 (localStorage)
│   │   ├── useResourceMap.ts     # 批量资源加载映射
│   │   ├── useSessions.ts        # 会话列表 (localStorage)
│   │   ├── useStorageList.ts     # 订阅式 localStorage 列表
│   │   └── useWorkDetail.ts      # 作品详情: manifest + preview_config
│   ├── lib/
│   │   ├── api.ts                # API 层: create/get job + SSE/WS + mock 模式
│   │   ├── errors.ts             # 错误解析/状态码判断
│   │   ├── manifestAssets.ts     # manifest 资源解析/摘要
│   │   ├── sessionActions.ts     # 新建会话/激活会话动作
│   │   ├── sessionDefaults.ts    # 默认选项/会话初始数据
│   │   ├── status.ts             # 状态标签/色调转换
│   │   └── storage.ts            # 本地存储: 会话/作品索引/广播
│   ├── pages/
│   │   ├── CreatePage.tsx        # 创作页: 聊天 + 参数/进度/结果
│   │   ├── DetailPage.tsx        # 作品详情路由包装
│   │   ├── LibraryPage.tsx       # 作品库: 搜索/筛选/卡片
│   │   ├── WorkDetailPage.tsx    # 作品详情: 预览 + 资源/状态
│   │   ├── create/
│   │   │   └── types.ts          # 创作页类型定义
│   │   ├── pages.css             # 页面级通用样式
│   │   └── workDetail.css        # WorkDetailPage 专用样式
│   └── types/
│       ├── job.ts                # Job 状态/事件 zod schema
│       ├── manifest.ts           # manifest schema + 字段归一化
│       ├── options.ts            # 创建任务 options 类型
│       └── previewConfig.ts      # preview_config schema
├── eslint.config.js              # ESLint 配置 (TS/React Hooks/Refresh)
├── index.html                    # HTML 模板
├── package.json                  # 前端依赖与脚本
├── package-lock.json             # 依赖锁定文件
├── README.md                     # 本地开发/Mock 模式说明
├── tsconfig.json                 # TypeScript 配置
└── vite.config.ts                # Vite 配置 (alias/proxy/base/env)
```
